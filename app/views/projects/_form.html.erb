<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :funding_ended_at %><br>
    <%= f.datetime_select :funding_ended_at %>
  </div>
  <div class="field">
    <%= f.label :funding_target_amount %><br>
    <%= f.text_field :funding_target_amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
